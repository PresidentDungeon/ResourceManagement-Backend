enum UserRoles{
  User = 'user',
  Admin = 'admin',
}

enum UserStatus{
  Pending = 'pending',
  Active = 'active',
  Approved = 'approved',
  Disabled = 'disabled',
}

enum ContractStatus{
  Request = 'request',
  Draft = 'draft',
  PendingReview = 'pending review',
  Expired = 'expired',
  Accepted = 'accepted',
  Rejected = 'rejected',
  Completed = 'completed',
}
